pipeline {
	agent {
		docker {image 'alpine/java:22-jdk'}
	}
	
	stages {
		stage('test-1') {
			steps {
				echo "You've reached stage=test-1 everything works fine!"
			}
		}
		stage('test-2') {
			steps {
				echo "You've reached stage=test-2 Hello There!"
				sh 'java --version'
			}
		}
	}
}
